package utils;

import org.junit.Assert;
import org.junit.Test;

import java.util.ArrayList;
import java.util.List;

import static utils.Interpretations.interpretations;

public class InterpretationsTest {

    @Test
    public void testInterpretations(){
        List<Integer> phoneNumber=new ArrayList<>();
        phoneNumber.add(210);
        phoneNumber.add(15);
        phoneNumber.add(65);
       // Assert.assertEquals(,interpretations(phoneNumber,false));
    }

}
